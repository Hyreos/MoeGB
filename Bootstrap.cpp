#include "Bootstrap.hpp"

#include <iostream>

 Bootstrap::Bootstrap(Cartrigbe cartrigbe) {
    start(cartrigbe);
 }

 void Bootstrap::start(Cartrigbe cartrigbe) {
     auto& data = cartrigbe.content();
 }